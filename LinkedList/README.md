- Arrays and vectors contigous memory location
- Linked list non-contigous
- [data, ptrToNextNode]
- For traverse -> Head Pointer (generally first node)
- 
```cpp
Node *ll = new Node(val); // persistent
Node ll(val); // static initialization (non persistant in memory)
```